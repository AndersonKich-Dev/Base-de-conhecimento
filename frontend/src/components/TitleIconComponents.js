import React from 'react'
import { TitleIconContainer } from './styles/styled-title-icon'

export default function TitleIcon({children}){


    return(
        <TitleIconContainer>
            {children}
        </TitleIconContainer>
    )
}